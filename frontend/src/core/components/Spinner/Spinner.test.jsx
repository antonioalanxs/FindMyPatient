import { render } from "@testing-library/react";

import Spinner from "@/core/components/Spinner/Spinner";

describe("Spinner", () => {
  it("should create", () => {
    render(<Spinner />);
  });
});
