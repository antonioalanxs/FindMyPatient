import { render } from "@testing-library/react";

import Spinner from "@/shared/components/Spinner/Spinner";

describe("Spinner", () => {
  it("should create", () => {
    render(<Spinner />);
  });
});
