import { render } from "@testing-library/react";

import Phrases from "@/shared/components/Load/Phrases/Phrases";

describe("Phrases", () => {
  it("should create", () => {
    render(<Phrases />);
  });
});
