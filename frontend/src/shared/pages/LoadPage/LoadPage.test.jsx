import { render } from "@testing-library/react";

import LoadPage from "@/shared/pages/LoadPage/LoadPage";

describe("LoadPage", () => {
  it("should create", () => {
    render(<LoadPage />);
  });
});
