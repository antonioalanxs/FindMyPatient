// libraries
import { render } from "@testing-library/react";

// components
import Error from "@/components/Error/Error";

describe("Error", () => {
  it("should create", () => {
    render(<Error />);
  });
});
